<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Studio 99 - 1v1 Arena</title>
  <style>
    canvas { background: #2e2e2e; display: block; margin: 40px auto; border: 3px solid #fff; }
    body { background: #111; color: white; font-family: Arial; text-align: center; }
    h1 { margin-top: 20px; }
  </style>
</head>
<body>
  <h1>Welcome to Studio 99 - 1v1 Arena</h1>
  <p>Use WASD for Player 1, Arrow Keys for Player 2</p>
  <canvas id="gameCanvas" width="800" height="600"></canvas>
  <script>
    const canvas = document.getElementById("gameCanvas");
    const ctx = canvas.getContext("2d");

    const player1 = { x: 100, y: 300, color: "blue", keys: { left: "a", right: "d", up: "w", down: "s" } };
    const player2 = { x: 700, y: 300, color: "red", keys: { left: "ArrowLeft", right: "ArrowRight", up: "ArrowUp", down: "ArrowDown" } };

    const keysPressed = {};

    window.addEventListener("keydown", e => keysPressed[e.key] = true);
    window.addEventListener("keyup", e => keysPressed[e.key] = false);

    function movePlayer(p) {
      if (keysPressed[p.keys.left]) p.x -= 5;
      if (keysPressed[p.keys.right]) p.x += 5;
      if (keysPressed[p.keys.up]) p.y -= 5;
      if (keysPressed[p.keys.down]) p.y += 5;
    }

    function drawPlayer(p) {
      ctx.fillStyle = p.color;
      ctx.fillRect(p.x - 15, p.y - 15, 30, 30);
    }

    function gameLoop() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      movePlayer(player1);
      movePlayer(player2);
      drawPlayer(player1);
      drawPlayer(player2);
      requestAnimationFrame(gameLoop);
    }

    gameLoop();
  </script>
</body>
</html>
